{
  "name": "agent-board",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["server", "tui"],
  "scripts": {
    "dev": "bash -c 'bun run --filter server dev & SERVER_PID=$!; trap \"kill $SERVER_PID 2>/dev/null\" EXIT INT TERM; cd tui && bun run dev'",
    "build": "bun run --filter '*' build",
    "install:all": "bun install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
