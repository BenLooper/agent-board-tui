{
  "name": "agent-board",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["server", "tui"],
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "build": "bun run --filter '*' build",
    "install:all": "bun install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
